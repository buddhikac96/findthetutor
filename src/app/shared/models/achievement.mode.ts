export class Achievement{
    url: string;
    name: string;
    content: string;

    constructor(url, name, content){
        this.url = url;
        this.name = name;
        this.content = content;
    }
}